import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'estg.ei.gourmetgo',
  appName: 'GourmetGo',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
